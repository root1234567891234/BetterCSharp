static string split_sum(string[] content,int num)
    {
        int i = 0;
        string output = "";
        foreach (string line in content) {
            if (a >= num)
            {
                output += line + " ";
                }
            i++;
            }    
        return output;
    }
